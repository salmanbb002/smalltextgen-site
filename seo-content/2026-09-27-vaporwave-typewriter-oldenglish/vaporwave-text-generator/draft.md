# Vaporwave Text Generator

Shipped directly into `lib/gallery-pages.ts` (entry `slug: "vaporwave-text-generator"`) + route `app/tools/vaporwave-text-generator/page.tsx` — that entry is the source of truth for the final copy (intro, 4 steps, 3 use cases, 10 FAQs).
