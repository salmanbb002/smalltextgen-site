# Typewriter Font Generator

Shipped directly into `lib/gallery-pages.ts` (entry `slug: "typewriter-font-generator"`) + route `app/tools/typewriter-font-generator/page.tsx` — that entry is the source of truth for the final copy (intro, 4 steps, 3 use cases, 10 FAQs).
