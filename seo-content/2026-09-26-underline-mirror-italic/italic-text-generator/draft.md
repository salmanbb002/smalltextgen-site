# Italic Text Generator — content for `/tools/italic (NEW pillar tool page; `italic` style already existed in lib/fonts.ts)`

Shipped directly into `lib/pillar-content.ts` (entry `slug: "italic"`) — that file is the source of truth for the final copy.

**Changes:** toolPages += italic (style already existed) · new pillar entry (intro, 4 steps, 3 whereUsed, 4 nativeFormatting, 10 FAQ) · pillarNav, home focusedTools, footer link · unit test for ℎ
