# Mirror Text Generator — content for `/tools/mirror (NEW pillar tool page + new `mirror` transform in lib/fonts.ts)`

Shipped directly into `lib/pillar-content.ts` (entry `slug: "mirror"`) — that file is the source of truth for the final copy.

**Changes:** NEW style `mirror` in lib/fonts.ts (conservative map — only clean mirrored glyphs; f g h j y G K Q + digits unchanged) + unit test · toolPages += mirror · new pillar entry (intro, 4 steps, 3 whereUsed, 10 FAQ) · pillarNav, home focusedTools, footer link
